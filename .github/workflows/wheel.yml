name: wheel

# maturin + PyO3 ベースの wheel ビルドと公開。
# develop と feature/** への push で自動テストする。
# タグ 202* が push されたら PyPI と GitHub Release に公開する。
on:
  workflow_dispatch:
  push:
    branches:
      - develop
      - "feature/**"
    paths:
      - "src/**"
      - "python/**"
      - "tests/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "build.rs"
      - "pyproject.toml"
      - ".github/workflows/wheel.yml"
    tags:
      - "202*"

permissions:
  contents: write
  actions: read

jobs:
  # ==========================================================================
  # abi3 wheel: Python 3.12+ GIL 有効ビルドで 1 wheel が 3.12/3.13/3.14 全てで使える
  # ==========================================================================
  # macos-15 と macos-26 は同一 target (aarch64-apple-darwin) のため wheel ファイル名も
  # 同一になる。同名 wheel を複数リリースすると PyPI / GitHub Release で衝突するので、
  # リリース用 artifact は古い SDK でビルドされる macos-15 のみとし、macos-26 は
  # ビルド + テスト専用 (upload: false) とする。
  build_abi3_macos:
    strategy:
      fail-fast: false
      matrix:
        platform:
          - name: macos-26_arm64
            runs_on: macos-26
            target: aarch64-apple-darwin
            upload: false
          - name: macos-15_arm64
            runs_on: macos-15
            target: aarch64-apple-darwin
            upload: true
    runs-on: ${{ matrix.platform.runs_on }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: false
          python-version: "3.12"
      - name: Build wheel (abi3)
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out wheelhouse --generate-stubs --compatibility off
          rust-toolchain: stable
      - name: Test wheel on Python 3.12/3.13/3.14
        run: |
          for py in 3.12 3.13 3.14; do
            uv venv --python "$py" ".venv-$py"
            uv pip install --python ".venv-$py/bin/python" wheelhouse/*.whl pytest pytest-timeout hypothesis
            NO_UV_SYNC=1 ".venv-$py/bin/pytest" tests/ --timeout=30 -q
          done
      - if: matrix.platform.upload
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: wheels-${{ matrix.platform.name }}_abi3
          path: wheelhouse/*.whl

  build_abi3_ubuntu:
    strategy:
      fail-fast: false
      matrix:
        target:
          - runs_on: ubuntu-24.04
            arch: x86_64
          - runs_on: ubuntu-24.04-arm
            arch: aarch64
    runs-on: ${{ matrix.target.runs_on }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Build wheel (abi3, manylinux2014)
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          target: ${{ matrix.target.arch }}
          args: --release --out wheelhouse --generate-stubs --compatibility manylinux2014
          manylinux: auto
          rust-toolchain: stable
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: false
          python-version: "3.14"
      - name: Test wheel
        run: |
          uv venv --python 3.14 .venv
          uv pip install --python .venv/bin/python wheelhouse/*.whl pytest pytest-timeout hypothesis
          NO_UV_SYNC=1 .venv/bin/pytest tests/ --timeout=30 -q
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: wheels-ubuntu_${{ matrix.target.arch }}_abi3
          path: wheelhouse/*.whl

  build_abi3_windows:
    runs-on: windows-2025
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: false
          python-version: "3.12"
      - name: Build wheel (abi3)
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          target: x86_64-pc-windows-msvc
          args: --release --out wheelhouse --generate-stubs --compatibility off
          rust-toolchain: stable
      - name: Test wheel
        shell: bash
        run: |
          uv venv --python 3.12 .venv
          uv pip install --python .venv/Scripts/python.exe wheelhouse/*.whl pytest pytest-timeout hypothesis
          NO_UV_SYNC=1 .venv/Scripts/pytest tests/ --timeout=30 -q
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: wheels-windows_x86_64_abi3
          path: wheelhouse/*.whl

  # ==========================================================================
  # Free-Threading wheel: Python 3.14t 専用 (PyO3 0.29 は 3.13t 非対応)
  # abi3 が使えないため Python バージョン毎のビルドが必要
  # ==========================================================================
  # abi3 と同様、リリース用 artifact は macos-15 のみとし macos-26 はテスト専用とする。
  build_ft_macos:
    strategy:
      fail-fast: false
      matrix:
        platform:
          - name: macos-26_arm64
            runs_on: macos-26
            target: aarch64-apple-darwin
            upload: false
          - name: macos-15_arm64
            runs_on: macos-15
            target: aarch64-apple-darwin
            upload: true
    runs-on: ${{ matrix.platform.runs_on }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: false
          python-version: "3.14t"
      - name: Build wheel (Free-Threading)
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          target: ${{ matrix.platform.target }}
          args: >-
            --release --out wheelhouse --generate-stubs --compatibility off
            --interpreter python3.14t
          rust-toolchain: stable
      - name: Test wheel (3.14t)
        run: |
          uv venv --python 3.14t .venv-ft
          uv pip install --python .venv-ft/bin/python wheelhouse/*.whl pytest pytest-timeout
          NO_UV_SYNC=1 .venv-ft/bin/pytest tests/test_free_threading.py tests/test_mp4.py --timeout=30 -q --noconftest
      - if: matrix.platform.upload
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: wheels-${{ matrix.platform.name }}_ft-3.14t
          path: wheelhouse/*.whl

  build_ft_ubuntu:
    strategy:
      fail-fast: false
      matrix:
        target:
          - runs_on: ubuntu-24.04
            arch: x86_64
          - runs_on: ubuntu-24.04-arm
            arch: aarch64
    runs-on: ${{ matrix.target.runs_on }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: false
          python-version: "3.14t"
      - name: Build wheel (Free-Threading, manylinux2014)
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          target: ${{ matrix.target.arch }}
          args: >-
            --release --out wheelhouse --generate-stubs --compatibility manylinux2014
            --interpreter python3.14t
          manylinux: auto
          rust-toolchain: stable
      - name: Test wheel (3.14t)
        run: |
          uv venv --python 3.14t .venv-ft
          uv pip install --python .venv-ft/bin/python wheelhouse/*.whl pytest pytest-timeout
          NO_UV_SYNC=1 .venv-ft/bin/pytest tests/test_free_threading.py tests/test_mp4.py --timeout=30 -q --noconftest
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: wheels-ubuntu_${{ matrix.target.arch }}_ft-3.14t
          path: wheelhouse/*.whl

  build_ft_windows:
    runs-on: windows-2025
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: false
          python-version: "3.14t"
      - name: Build wheel (Free-Threading)
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          target: x86_64-pc-windows-msvc
          args: >-
            --release --out wheelhouse --generate-stubs --compatibility off
            --interpreter python3.14t
          rust-toolchain: stable
      - name: Test wheel (3.14t)
        shell: bash
        run: |
          uv venv --python 3.14t .venv-ft
          uv pip install --python .venv-ft/Scripts/python.exe wheelhouse/*.whl pytest pytest-timeout
          NO_UV_SYNC=1 .venv-ft/Scripts/pytest tests/test_free_threading.py tests/test_mp4.py --timeout=30 -q --noconftest
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: wheels-windows_x86_64_ft-3.14t
          path: wheelhouse/*.whl

  # ==========================================================================
  # sdist (ソース配布)
  # ==========================================================================
  build_sdist:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Build sdist
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          command: sdist
          args: --out wheelhouse
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sdist
          path: wheelhouse/*.tar.gz

  # ==========================================================================
  # PyPI 公開 (タグ 202* のみ)
  # ==========================================================================
  publish_pypi:
    if: contains(github.ref, 'tags/202')
    needs:
      - build_abi3_macos
      - build_abi3_ubuntu
      - build_abi3_windows
      - build_ft_macos
      - build_ft_ubuntu
      - build_ft_windows
      - build_sdist
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: wheels-*
          path: dist/
          merge-multiple: true
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: sdist
          path: dist/
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@ba38be9e461d3875417946c167d0b5f3d385a247 # v1.14.1
        with:
          packages-dir: dist

  # ==========================================================================
  # GitHub Release 作成 (タグ 202* のみ)
  # ==========================================================================
  create_release:
    if: contains(github.ref, 'tags/202')
    needs:
      - build_abi3_macos
      - build_abi3_ubuntu
      - build_abi3_windows
      - build_ft_macos
      - build_ft_ubuntu
      - build_ft_windows
      - build_sdist
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: artifacts/
      - name: Collect release files
        run: |
          find artifacts -type f \( -name "*.whl" -o -name "*.tar.gz" \) > release_files.txt
          wc -l release_files.txt
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [[ "${{ github.ref }}" == *"dev"* ]]; then
            gh release create ${{ github.ref_name }} \
              --title "${{ github.ref_name }}" \
              --prerelease \
              $(cat release_files.txt | xargs)
          else
            gh release create ${{ github.ref_name }} \
              --title "${{ github.ref_name }}" \
              $(cat release_files.txt | xargs)
          fi
